class Shape {
    constructor(area, perimeter) {
        this.area = area;
        this.perimeter = perimeter;
    }
    //if shape is triangle then make triangle
    //if shape is a circle, then make circle
    //if shape is a square, make square

    //function for making color

    //function for making text and text color
    //text generation
    // <text x="10" y="10" fill="blue">Hello World!</text>
}

module.exports = Shape;